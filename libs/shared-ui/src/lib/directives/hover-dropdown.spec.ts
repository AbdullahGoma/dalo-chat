import { HoverDropdown } from './hover-dropdown';

describe('HoverDropdown', () => {
  it('should create an instance', () => {
    const directive = new HoverDropdown();
    expect(directive).toBeTruthy();
  });
});
